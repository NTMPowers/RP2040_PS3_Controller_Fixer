# RP2040 PS3 Controller Fixer

Firmware for an RP2040 board that emulates a DualShock 3 controller for PS3 to fix the black screen problem when mounting a ISO without a controller connected. For example using only the BD remote or the virtual pad.

Run `build.bat` to create the CFW and PS3HEN UF2 files in `output`.
